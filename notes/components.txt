we are not using mui datepicker because the pickerCalendarHeader is always one month prior the default value and the date is always the last day of the month. We replace the datepicker component with date field. 