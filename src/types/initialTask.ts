export interface InitialTask {
    id: string
    summary: string
    description: string
    status: string
    priority: string
    issueType: string
    dueDate: string
    labels: string[]
    attachments: number[]
  }